spring:
  config:
    activate.on-profile: test

  datasource:
    url: jdbc:postgresql://localhost:5433/CableCashTestDB
    username: test
    password: test
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
